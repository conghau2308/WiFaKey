<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WifakeyApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WifakeyApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WifakeyApp"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="WifakeyApp">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Grid Grid.Row="1">
            <MediaPlayerElement x:Name="PreviewPlayer" Stretch="UniformToFill"/>
            <ScrollViewer VerticalAlignment="Bottom">
                <Border Margin="16" CornerRadius="8" Padding="12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#00000000" Offset="0.0"/>
                            <GradientStop Color="#CC000000" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock x:Name="ResultText" TextWrapping="Wrap"
                               Foreground="White"/>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>